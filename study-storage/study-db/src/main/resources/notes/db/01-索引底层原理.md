# 01-索引底层原理

## B+Tree 结构

```mermaid
graph TD
    subgraph "B+Tree 3层结构"
        ROOT["[ROOT] 非叶子节点<br/>key: 50"]
        L1_N1["[Level1] 非叶子节点<br/>key: 10, 30"]
        L1_N2["[Level1] 非叶子节点<br/>key: 70, 90"]
        L2_N1["[Leaf] key: 1,5,10<br/> -> next"]
        L2_N2["[Leaf] key: 15,20,30<br/> -> next"]
        L2_N3["[Leaf] key: 35,40,50<br/> -> next"]
        L2_N4["[Leaf] key: 55,60,70<br/> -> next"]
        L2_N5["[Leaf] key: 75,80,90<br/> -> next"]
        L2_N6["[Leaf] key: 95,100<br/> -> NULL"]
    end

    ROOT --> L1_N1
    ROOT --> L1_N2
    L1_N1 --> L2_N1
    L1_N1 --> L2_N2
    L1_N1 --> L2_N3
    L1_N2 --> L2_N4
    L1_N2 --> L2_N5
    L1_N2 --> L2_N6
    L2_N1 -.-> L2_N2
    L2_N2 -.-> L2_N3
    L2_N3 -.-> L2_N4
    L2_N4 -.-> L2_N5
    L2_N5 -.-> L2_N6
```

### B+Tree 核心特性

| 特性 | 说明 |
|------|------|
| 数据只存叶子节点 | 非叶子节点只存 key，不存 data |
| 叶子节点有序链表 | 支持范围查找，顺序遍历 |
| 树高度稳定 | 通常 2-4 层，千万级数据 3 层足够 |
| 每个节点多 key | 通常 16KB 页存约 1000 个 key |

### 千亿数据 3-4 层够用计算

```
一个 16KB 的 InnoDB 页:
  - 主键 BIGINT (8字节) + 指针 (6字节) = 14字节/条目
  - 每页约 16KB / 14B ≈ 1170 个索引条目
  - 叶子节点: 每行约 1KB，每页约 16 行

层数计算:
  第1层(root): 1 页, 1170 个指针
  第2层: 1170 页, 1170*1170 ≈ 137万 个指针
  第3层(叶子): 137万 页, 137万*16 ≈ 2190万 行数据

结论: 3 层 B+Tree 可存储约 2000 万行记录
      4 层 B+Tree 可存储约 256 亿行记录
```

## 聚簇索引 vs 非聚簇索引 vs 覆盖索引

```mermaid
graph LR
    subgraph "聚簇索引查询"
        PK1["WHERE id=3"] --> ROOT1["B+Tree(PRIMARY)"]
        ROOT1 --> LEAF1["叶子节点: 整行数据"]
    end

    subgraph "非聚簇索引查询(回表)"
        SEC1["WHERE name='Alice'"] --> IDX1["B+Tree(idx_name)"]
        IDX1 --> LEAF2["叶子节点: pk=100"]
        LEAF2 --> ROOT2["B+Tree(PRIMARY) 回表"]
        ROOT2 --> LEAF3["叶子节点: 整行数据"]
    end

    subgraph "覆盖索引查询(不回表)"
        COV1["SELECT name,age<br/>WHERE name='Alice'"] --> IDX2["B+Tree(idx_name_age)"]
        IDX2 --> LEAF4["叶子节点: name,age 直接返回"]
    end

    style PK1 fill:#90EE90
    style LEAF1 fill:#90EE90
    style SEC1 fill:#FFB347
    style LEAF2 fill:#FFB347
    style LEAF3 fill:#FFB347
    style COV1 fill:#87CEEB
    style LEAF4 fill:#87CEEB
```

| 对比项 | 聚簇索引 | 非聚簇索引 | 覆盖索引 |
|--------|----------|------------|----------|
| 叶子存储 | 整行数据 | 主键值 | 查询所需列 |
| 每表数量 | 仅 1 个 | 多个 | 非独立类型 |
| IO 次数 | 1 次 | >= 2 次 | 1 次 |
| Extra 标识 | - | Using index condition | Using index |

## 最左前缀原则 + ICP 下推

```mermaid
flowchart TD
    START["查询: WHERE a=? AND b>? AND c=?"]
    A["索引定位 a=? (二分查找)"]
    B{"b 是范围条件?"}
    C["b>? 范围扫描, 找到所有匹配 a=? 的行"]
    D["c=? 在索引中检查 (ICP下推到引擎层)"]
    E["满足 c=? 的行回表取完整数据"]
    F["返回结果"]

    START --> A
    A --> B
    B -->|YES| C
    C --> D
    D --> E
    E --> F
    B -->|NO| X["b=? 等值继续二分"]
    X --> Y["继续用 c=? 定位"]
    Y --> E

    style START fill:#e1f5fe
    style D fill:#c8e6c9
    style F fill:#e1f5fe
```

### 最左前缀口诀

```
索引 (a, b, c):

age>20         -> range        (不用索引排序)

= 等值匹配，范围匹配不用索引排序
范围匹配不用索引排序
范围匹配不用索引排序a = ?                     -> ref          (全部命中)
a = ? AND b = ?            -> ref          (全部命中，b紧跟a)
a = ? AND b = ? AND c = ?  -> ref/const    (全部命中)
a = ? AND c = ?            -> ref(a)       (a可用，c跳过b失效)
b = ? AND c = ?            -> index/ALL    (跳过a，全失效)
a = ? AND b > ? AND c = ?  -> ref(a)+range(b) (a=ref, b=range, c失效)
a = ? AND b = ? ORDER BY c -> ref(a,b)+file sort(c排序走索引)
ORDER BY a,b,c             -> index        (全排序走索引)
```

### ICP 下推关键点

- MySQL 5.6+ 支持
- 存储引擎在索引遍历时直接过滤 WHERE 条件
- 减少回表次数，降低 Server 层与引擎层数据传输
- 适用条件：`Using index condition`，需要索引覆盖 WHERE 的部分列