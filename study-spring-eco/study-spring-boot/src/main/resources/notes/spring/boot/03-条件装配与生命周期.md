# Spring Boot 条件装配与生命周期

## 一、@Conditional 注解矩阵

```mermaid
flowchart TD
    subgraph Conditions["条件装配注解分类"]
        direction TB

        subgraph ClassPath["类路径条件"]
            OnClass["@ConditionalOnClass<br/>有某类时才装配"]
            OnMissingClass["@ConditionalOnMissingClass<br/>缺少某类时才装配"]
        end

        subgraph BeanCheck["Bean 存在性条件"]
            OnBean["@ConditionalOnBean<br/>有某 Bean 时才装配"]
            OnMissingBean["@ConditionalOnMissingBean<br/>无某 Bean 时才装配"]
        end

        subgraph Property["属性条件"]
            OnProperty["@ConditionalOnProperty<br/>配置属性匹配时装配"]
            OnExpression["@ConditionalOnExpression<br/>SpEL 表达式为 true 时装配"]
        end

        subgraph Environment["环境条件"]
            OnWeb["@ConditionalOnWebApplication<br/>Web 环境时装配"]
            OnNotWeb["@ConditionalOnNotWebApplication<br/>非 Web 环境时装配"]
            OnResource["@ConditionalOnResource<br/>资源文件存在时装配"]
        end
    end

    style ClassPath fill:#fff3e0,stroke:#e65100
    style BeanCheck fill:#e8f5e9,stroke:#2e7d32
    style Property fill:#e3f2fd,stroke:#1565c0
    style Environment fill:#fce4ec,stroke:#c62828
```

## 二、条件装配优先级与短路求值

```mermaid
flowchart LR
    A["候选配置类"] --> B{"@ConditionalOnClass"}
    B -- "类不存在" --> X["跳过"]
    B -- "类存在" --> C{"@ConditionalOnMissingBean"}
    C -- "Bean 已存在" --> X
    C -- "Bean 不存在" --> D{"@ConditionalOnProperty"}
    D -- "属性不匹配" --> X
    D -- "属性匹配" --> E{"其他条件..."}
    E -- "不满足" --> X
    E -- "满足" --> F["注册 Bean"]

    style A fill:#4fc08d,color:#fff
    style F fill:#4fc08d,color:#fff
    style X fill:#ff5252,color:#fff
```

**核心规则：**
- 多个 `@ConditionalOnXxx` 之间是 **AND** 关系
- 任何一个条件不满足，整个配置类被跳过
- 这体现了 **短路求值**（short-circuit）思想
- 优先级：`@ConditionalOnClass` > `@ConditionalOnMissingBean` > `@ConditionalOnProperty` > `@Profile`

## 三、Spring Boot 生命周期时序图

```mermaid
sequenceDiagram
    participant Main as main()
    participant App as SpringApplication
    participant Env as Environment
    participant Ctx as ApplicationContext
    participant Bean as Bean 实例
    participant Runner as Runner

    Main->>App: new SpringApplication(主类)
    Note over App: 推断 Web 类型<br/>设置 Initializer/Listener

    Main->>App: run(args)
    App->>Env: 准备 Environment
    Env-->>App: PropertySources 就绪

    App->>App: 打印 Banner
    App->>Ctx: 创建 ApplicationContext
    Note over Ctx: 根据 Web 类型选择<br/>AnnotationConfig / Servlet / Reactive

    App->>Ctx: 准备 Context
    Ctx->>Env: 绑定 Environment
    App->>Ctx: 注册 Configuration 类

    Ctx->>Ctx: refresh()
    Ctx->>Ctx: BeanFactoryPostProcessor 执行
    Ctx->>Ctx: Bean 实例化与依赖注入
    Ctx->>Bean: @PostConstruct 回调

    Ctx->>Ctx: 内嵌容器启动（Web 模式）
    Note over Ctx: Tomcat/Jetty/Undertow 启动

    Ctx->>Runner: ApplicationRunner.run()
    Runner-->>Ctx: 执行完成
    Ctx->>Runner: CommandLineRunner.run()
    Runner-->>Ctx: 执行完成

    Ctx-->>App: 容器就绪

    Note over Main,App: === 应用正常运行中 ===

    Main->>App: 收到关闭信号
    App->>Ctx: ContextClosedEvent
    Ctx->>Ctx: 停止接收新请求
    Ctx->>Ctx: 等待进行中请求完成
    Ctx->>Bean: @PreDestroy 回调
    Ctx->>Ctx: DisposableBean.destroy()
    Ctx->>Ctx: 关闭内嵌容器
    Ctx-->>App: 容器已关闭
    App-->>Main: JVM 退出
```

## 四、ApplicationRunner vs CommandLineRunner

| 对比维度 | ApplicationRunner | CommandLineRunner |
|----------|-------------------|-------------------|
| 参数类型 | `ApplicationArguments`（解析后） | `String[] args`（原始） |
| 参数解析 | 自动分离 optionArgs / nonOptionArgs | 需要手动解析 |
| 适用场景 | 需要结构化的参数访问 | 简单场景或原始参数处理 |
| 执行顺序 | 先于 CommandLineRunner | 后于 ApplicationRunner |
| 多实例排序 | `@Order` 控制 | `@Order` 控制 |

```java
// ApplicationRunner -- 适合需要解析参数的场景
@Bean
ApplicationRunner appRunner() {
    return args -> {
        args.getOptionNames();         // ["server.port"]
        args.getOptionValues("server.port"); // ["8080"]
        args.getNonOptionArgs();       // []
    };
}

// CommandLineRunner -- 适合简单拿原始 args
@Bean
CommandLineRunner cmdRunner() {
    return args -> {
        System.out.println(Arrays.toString(args));
        // ["--server.port=8080", "--app.name=demo"]
    };
}
```

## 五、SpringApplication 自定义

| 配置项 | 方法 | 说明 |
|--------|------|------|
| Banner 模式 | `setBannerMode(Banner.Mode.OFF)` | 关闭启动 Banner |
| Web 类型 | `setWebApplicationType(NONE)` | 不启动内嵌容器 |
| 启动日志 | `setLogStartupInfo(false)` | 关闭启动信息日志 |
| Bean 覆盖 | `setAllowBeanDefinitionOverriding(true)` | 允许同名 Bean 覆盖 |
| 监听器 | `addListeners(...)` | 添加自定义 ApplicationListener |
| 初始化器 | `addInitializers(...)` | 添加 ApplicationContextInitializer |

## 六、内嵌 Tomcat 启动证明

**内嵌容器调用链（源码层面）：**

```
SpringApplication.run()
  → refreshContext(context)
    → ServletWebServerApplicationContext.onRefresh()
      → createWebServer()
        → getWebServerFactory()
          → TomcatServletWebServerFactory.getWebServer()
            → new Tomcat()             ← org.apache.catalina.startup.Tomcat
            → tomcat.start()           ← 启动内嵌容器
```

**判断依据：**

1. 不依赖外部 Tomcat 安装 → 独立 JAR 可直接运行
2. `spring-boot-starter-web` 依赖 `spring-boot-starter-tomcat`
3. 控制台日志: `o.s.b.w.embedded.tomcat.TomcatWebServer`
4. 可通过 `spring-boot-starter-undertow/jetty` 替换

## 七、优雅关闭机制

### 配置

```yaml
server:
  shutdown: graceful                     # 开启优雅关闭
spring:
  lifecycle:
    timeout-per-shutdown-phase: 30s     # 超时 30 秒
```

### 关闭流程

```
1. 收到关闭信号（SIGTERM / kill / context.close()）
2. Spring Boot 发布 ContextClosedEvent
3. 停止接收新请求（Web 容器层面）
4. 等待进行中的请求完成（server.shutdown=graceful）
5. 超时控制（spring.lifecycle.timeout-per-shutdown-phase）
6. @PreDestroy 回调
7. DisposableBean.destroy() 回调
8. 关闭内嵌容器
9. 关闭 ApplicationContext
10. JVM 退出
```