# 02-任务调度与容错

## Master-Worker 架构

```mermaid
graph TD
    subgraph User["用户 / 外部系统"]
        UI["Web UI / API 提交"]
    end

    subgraph MasterCluster["Master 集群 (HA)"]
        AM["Active Master<br/>(持有 ZK 锁, 调度决策)"]
        SM["Standby Master<br/>(监听锁, 待接管)"]
    end

    subgraph ZK["ZooKeeper 集群"]
        LOCK["/locks/master<br/>(分布式锁)"]
        WORKERS["/nodes/worker/*<br/>(Worker 心跳)"]
        TASKS["/tasks/queue<br/>(任务队列)"]
    end

    subgraph WorkerCluster["Worker 集群"]
        W1["Worker-1<br/>TaskProcessor<br/>Shell/SQL/Python..."]
        W2["Worker-2<br/>TaskProcessor"]
        W3["Worker-3<br/>TaskProcessor"]
    end

    UI -->|"1. 提交工作流"| AM
    AM -->|"2. 抢占锁"| LOCK
    AM -->|"3. 拓扑排序, 任务分发"| W1
    AM -->|"3. 拓扑排序, 任务分发"| W2
    AM -->|"3. 拓扑排序, 任务分发"| W3
    W1 -->|"4. 心跳 (10s)"| WORKERS
    W2 -->|"4. 心跳"| WORKERS
    W3 -->|"4. 心跳"| WORKERS
    W1 -->|"5. 执行结果回传"| AM
    W2 -->|"5. 执行结果回传"| AM
    W3 -->|"5. 执行结果回传"| AM
    AM -.->|"Active 宕机"| SM
    SM -.->|"抢锁升级"| LOCK
```

## 任务分片机制

```mermaid
sequenceDiagram
    participant User as 用户
    participant Master as Master
    participant W1 as Worker-1
    participant W2 as Worker-2
    participant W3 as Worker-3

    User->>Master: 提交 ETL 任务 (shardCount=3)
    Master->>Master: 生成 3 个 TaskInstance<br/>(shard-0, shard-1, shard-2)
    Master->>W1: 分发 shard-0<br/>"WHERE MOD(hash(id),3)=0"
    Master->>W2: 分发 shard-1<br/>"WHERE MOD(hash(id),3)=1"
    Master->>W3: 分发 shard-2<br/>"WHERE MOD(hash(id),3)=2"
    
    Note over W1,W3: 3 个 Worker 并行处理各自分片

    W1-->>Master: shard-0 SUCCESS
    W2-->>Master: shard-1 SUCCESS
    W3-->>Master: shard-2 SUCCESS
    Master-->>User: ETL 任务全部完成 (3 分片)
```

## Cron 表达式字段含义

```mermaid
graph LR
    subgraph Fields["Cron 字段 (6位)"]
        F1["秒<br/>0-59"]
        F2["分钟<br/>0-59"]
        F3["小时<br/>0-23"]
        F4["日<br/>1-31"]
        F5["月<br/>1-12"]
        F6["星期<br/>1-7<br/>(1=周日)"]
    end

    F1 --> F2 --> F3 --> F4 --> F5 --> F6
```

| 表达式 | 含义 |
|--------|------|
| `0 0 2 * * ?` | 每天凌晨 2:00 |
| `0 */5 * * * ?` | 每 5 分钟 |
| `0 0 9-18/2 * * ?` | 9:00-18:00 每 2 小时 |
| `0 30 1 1,15 * ?` | 每月 1 号和 15 号 1:30 |
| `0 0 8 ? * MON-FRI` | 周一到周五 8:00 |

## 容错机制 4 层

```mermaid
flowchart TD
    Task["任务开始执行"] --> L1{"第1层: 任务重试<br/>retryTimes + retryInterval"}
    L1 -->|"重试成功"| Success["SUCCESS"]
    L1 -->|"重试耗尽"| L2{"第2层: 超时Kill<br/>timeout + timeoutStrategy"}
    L2 -->|"超时 Kill"| Failed["FAILED"]
    L2 -->|"正常失败"| Failed
    Failed --> L3{"第3层: 手动恢复<br/>UI 点击恢复"}
    L3 -->|"管理员操作"| Retry["重新调度"]
    Retry --> Task

    subgraph WorkerFail["Worker 宕机场景"]
        WF1["Worker 心跳中断"] --> ZK["ZK ephemeral 节点过期"]
        ZK --> MasterDetect["Master Watcher 感知"]
        MasterDetect --> Failover["第4层: 故障转移"]
        Failover --> Tag["标记 NEED_FAULT_TOLERANCE"]
        Tag --> Reassign["重新分配到健康 Worker"]
    end
```

## 任务状态机

```mermaid
stateDiagram-v2
    [*] --> SUBMITTED: 提交任务
    SUBMITTED --> RUNNING: Master 分发到 Worker
    RUNNING --> SUCCESS: 执行成功
    SUCCESS --> [*]

    RUNNING --> FAILURE: 执行异常
    FAILURE --> RUNNING: 重试 (retryTimes 内)
    FAILURE --> [*]: 重试耗尽

    RUNNING --> TIMEOUT: 超时 Kill
    TIMEOUT --> RUNNING: 手动恢复

    RUNNING --> KILL: 手动 Kill/故障转移
    KILL --> [*]
```

## 优先级队列调度

```mermaid
graph LR
    subgraph Producer["任务提交"]
        T1["HIGH (权重3)"]
        T2["MEDIUM (权重2)"]
        T3["LOW (权重1)"]
    end

    subgraph Queue["PriorityBlockingQueue"]
        direction TB
        Q1["HIGH: 风控检测"]
        Q2["HIGH: 实时计算"]
        Q3["MEDIUM: 数据同步"]
        Q4["LOW: 日志归档"]
    end

    subgraph Consumer["Master 调度"]
        Scheduler["Worker Pool"]
    end

    T1 --> Queue
    T2 --> Queue
    T3 --> Queue
    Queue -->|"按权重降序出队"| Scheduler
```

## 面试要点

1. **Master 如何防止单点故障？** 通过 ZooKeeper 分布式锁实现 HA。Active Master 持有临时节点锁，宕机后锁过期，Standby Master 通过 Watcher 感知并抢锁升级。

2. **Worker 宕机后，正在执行的任务如何处理？** 
   - Master 监听到 Worker 心跳中断 (10s)
   - 将该 Worker 上所有 RUNNING 的 TaskInstance 标记 NEED_FAULT_TOLERANCE
   - 通过 RPC 调用旧 Worker 的 `killTask` 接口确保不重复执行
   - 将 NEED_FAULT_TOLERANCE 的任务重新分发到健康 Worker

3. **任务分片的数据倾斜如何处理？** 
   - 分片键选择：避免用自增 ID 分片 (会导致尾部热点)
   - hash 分片：`MOD(CRC32(bizKey), shardCount)`
   - 二次分片：如果某个分片数据量仍然过大，可单独对该分片再次分片
   - 动态分片：根据数据量动态调整分片数 (高级特性)

4. **Cron 表达式 vs 依赖触发，何时用哪个？**
   - Cron：定时批量任务 (T+1 日终批量)
   - 依赖触发：上游数据就绪后触发 (实时/准实时链路)